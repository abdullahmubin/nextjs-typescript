import { CarProps } from '@/types'
import React from 'react'

interface CarDetails {
    car: CarProps;
    isOpen: boolean;
    closeModal: () => void;
}
function CarDetails() {
    return (
        <div>

        </div>
    )
}

export default CarDetails
